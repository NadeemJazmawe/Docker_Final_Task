pipeline {

  agent any

  environment {
		DOCKERHUB_CREDENTIALS=credentials('dockerhub')
	}

  stages {

    stage('Login to dockerhub') {

			steps {
				sh 'sudo docker login -u $DOCKERHUB_CREDENTIALS_USR --password-stdin'
			}
		}

    stage('Build Image') {
      steps {
        sh 'docker build -t bitcoin-price:latest .'
      }
    }

    stage('Push Image') {
      steps {
			sh 'docker push nadeemjazmawe/bitcoin-price'
        }
      }
    }

    
}
